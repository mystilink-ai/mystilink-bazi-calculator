# Python binding

The primary Python package is the repository root package `mystilink_bazi`
(install with `python3 -m pip install -e .` from the repo root).

This directory re-exports the same API for language-matrix layout consistency.

```python
from mystilink_bazi_binding import compute_bazi, compute_dayun, compute_liunian
```
